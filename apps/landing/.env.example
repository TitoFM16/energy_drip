# Public origin used to generate canonical, Open Graph, sitemap, and robots URLs.
# Set this to the real HTTPS origin in production; do not include a trailing slash.
VITE_SITE_URL=https://your-public-domain.example

# API origin used by the public booking form.
VITE_API_BASE_URL=http://localhost:8000
